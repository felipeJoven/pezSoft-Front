import { Lote } from "./lote";

export class Pesca {
    id: number;    
    fechaCreacion: Date;
    pecesPescados: number;
    pesoPromedio: number;
    lote: Lote;
    loteId: number;
    loteLote: string;
}